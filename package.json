{
  "name": "sso-reporter-action",
  "version": "1.0.0",
  "description": "GitHub Action for running SEO Reporter",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": [
    "github-action",
    "seo",
    "crawler",
    "audit"
  ],
  "author": "Antler Digital",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/artifact": "^2.1.10",
    "seo-reporter": "^1.1.2",
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
